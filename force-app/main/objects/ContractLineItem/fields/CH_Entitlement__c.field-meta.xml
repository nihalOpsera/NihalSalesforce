<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CH_Entitlement__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Change for the integration / migration to correctly link CDB Service offering against the right Entitlement Id using lookup on Entitlement name in which we use the field Service Mapping Id (CDB sends Salesforce the Entitlement Process name). This is Salesforce Reference. Salesforce needs to trigger the linkage after the Entitlement object is processed.</description>
    <externalId>false</externalId>
    <label>Entitlement ID</label>
    <referenceTo>Entitlement</referenceTo>
    <relationshipLabel>Contract Line Items</relationshipLabel>
    <relationshipName>Contract_Line_Items</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>

<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Mendatory_Web_Email</fullName>
    <active>true</active>
    <description>If issue reported by is not available then web email is Mandatory in order to send end-user communication.</description>
    <errorConditionFormula>AND(
    RecordType.Name = &apos;Partner Support&apos;,
    ISBLANK(Issue_Reported_By__c),
    ISBLANK(SuppliedEmail)  
    )</errorConditionFormula>
    <errorDisplayField>SuppliedEmail</errorDisplayField>
    <errorMessage>Web Email is mandatory as Issue Reported By is not available.</errorMessage>
</ValidationRule>

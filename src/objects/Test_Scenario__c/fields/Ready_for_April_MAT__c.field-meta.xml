<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Ready_for_April_MAT__c</fullName>
    <externalId>false</externalId>
    <formula>IF(ISPICKVAL(Request__r.Status__c, &quot;Release: UAT&quot;), TRUE, 
  IF(ISPICKVAL(Request__r.Status__c, &quot;Release: E2E SIT&quot;), TRUE, 
    IF(ISPICKVAL(Request__r.Status__c, &quot;Release: E2E UAT&quot;), TRUE, 
      IF(ISPICKVAL(Request__r.Status__c, &quot;Close: Deployed&quot;), TRUE,
        IF(ISPICKVAL(Request__r.Status__c, &quot;Close: Duplicate&quot;), TRUE, FALSE)
      )
    )
  )
)</formula>
    <inlineHelpText>The Requirement Status indicates that the requirement has already undergone project team testing and Product Owner validation.</inlineHelpText>
    <label>Ready for April MAT?</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
